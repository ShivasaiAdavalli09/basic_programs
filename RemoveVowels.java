package q4interview.programs;

import java.util.ArrayList;

public class RemoveVowels {
	    //if we want to give user input
		//BufferedReader br = new BufferedReader(new InputStreamReader(System.in));<br />
	    //String str;<br />
	    //System.out.println("Enter the String : ");<br />
	    //str=br.readLine();<br />
	//ArrayList is a collection framework used in java that serves as dynamic data structure incorporating the traits of an array i.e.
	//Stores homogeneous elements
	//Elements referred by the array name and index value
	//Stores Elements in contiguous memory allocations
	public static void main(String args[]){
	String str="what is my name";
	String res;
	for(int i=0;i<str.length();i++){
		char ch=str.charAt(i);
		if(ch==('a'|'e'|'i'|'o'|'u')){
		
}
}
}}